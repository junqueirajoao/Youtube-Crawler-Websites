{
  "name": "crawler-sites",
  "version": "1.0.0",
  "description": "YouTube video ID crawler — checks a list of URLs and reports which ones embed a given video.",
  "type": "module",
  "scripts": {
    "start": "node Crawler-Sites.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "axios": "^1.20.0",
    "playwright": "^1.63.0",
    "playwright-extra": "^4.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  }
}
